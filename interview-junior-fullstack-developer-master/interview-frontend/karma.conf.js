module.exports = function (config) {
  config.set({
    // Other configurations...

    files: [
      'src/**/*.ts',
      'src/**/*.spec.ts',
      'src/**/*.html',
      'src/**/*.css',
    ],

    // Other configurations...
  });
};
